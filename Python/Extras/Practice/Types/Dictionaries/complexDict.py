#!/usr/bin/env python

complexDict = {'name': {'first': 'Bob', 'last': 'Smith'},
				'jobs': ['dev', 'mgr'],
				'age': 35.5
				}

print(complexDict)
print(complexDict['jobs'][1])
print(complexDict['name']['last'])